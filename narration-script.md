# Narration script — "Variables" (target ~3:55, calm tutorial pace)

Paste each block into ElevenLabs separately (or all at once — see README).
Use a calm, warm narrator voice. In ElevenLabs, good starting presets:
**"Adam"** or **"Brian"** (male, clear) or **"Rachel"** (female, clear) at
Stability ~50%, Similarity ~75%, Style ~20% (keeps it steady, not dramatic).

---

### Scene 1 — Hook (0:00–0:12)
Every app you use — Instagram, Spotify, your calculator app — is built on
one simple idea. It's called a variable. And in the next few minutes,
you'll actually understand it.

### Scene 2 — What is a variable (0:12–0:45)
Think of a variable as a labeled box. The label is its name. Whatever's
inside the box — a number, a word, anything — that's its value. You give
the box a name so you can find it again later, without caring exactly
where it lives in memory.

### Scene 3 — Declaring and assigning (0:45–1:35)
Creating one is called declaring. And putting something inside it is
called assigning. Watch: we write "let score equals zero." "Let"
declares the box. The equals sign isn't math here — it means "put this
value inside." Now score holds zero, and we can use that name anywhere
in our code.

### Scene 4 — Naming rules (1:35–2:05)
Names matter. "Score" tells you what's inside. "X" doesn't. The rules
are simple: no spaces, don't start with a number, and skip words your
language already uses, like "let" itself. Good names are the difference
between code you understand in six months, and code you have to relearn
from scratch.

### Scene 5 — Reassignment (2:05–2:45)
Here's the key idea in the name itself: variables can vary. The box
stays, the label stays — but what's inside can change. Score equals
zero. Then later, score equals ten. Same box. New value. The old value
is just... gone, replaced.

### Scene 6 — Data types (2:45–3:30)
And boxes don't just hold numbers. A name could hold text, like your
username. It could hold true or false, called a boolean — perfect for
yes-or-no questions like "is logged in." It could even hold a whole
list of things. Same idea every time: a name, pointing at a value.

### Scene 7 — Recap (3:30–3:55)
So: a variable is a labeled box. You declare it, you assign a value,
and that value can change any time. That's it — that's the idea every
single program is built on. Follow for the next concept: functions.

---

**Total word count: ~330 words.** At a calm ~110–130 wpm that lands
right around 3:30–3:55, which matches the scene timings in `tokens.ts`.
